# Фундаментальные библиотеки для работы с тензорами и ИИ (обычно уже есть в ComfyUI)
torch>=2.0.0
numpy>=1.22.0

# Обработка изображений и сохранение файлов в MultiMetricScorer
Pillow>=9.0.0

# Системные зависимости для работы с путями и регулярными выражениями
# (встроены в Python, в requirements.txt их писать НЕ нужно, например: os, csv, re, json, datetime)